# 工作说明

## 1. 文档说明

本文档用于总结本次 `ArceOS VirtIO` 相关开发工作的**最终成果**，以已经合并进上游仓库的 PR 为准，而不是按中间某一次本地修复状态或某个临时排查版本来写。

- 上游 PR：`#376`
- PR 标题：`增强 ArceOS 中 VirtIO Net、Vsock 及通用探测路径`
- 合并时间：`2026-05-14 07:19:47 UTC`
- GitHub merge commit：`90dd789fb4a4624d1fc923f73f94ffcd409059fa`
- 本地分支最终对应头提交：`42e76904f4abafbda0b7cbf2ba650a5df87b47b7`

说明：

- `42e76904f` 是本地分支在合并前吸收最新 `upstream/dev` 并解决冲突后的最终版本。
- GitHub 上游实际合并时生成了新的 merge commit `90dd789f...`，但代码语义与本地最终版本一致。
- 本文只总结**这条 PR 自身引入并最终被合入的工作**，不将中途为了本地定位 CI 或临时实验而做、且未进入最终代码的修改记入成果。

---

## 2. 总体目标

本次工作的核心目标，不是简单“新增一个设备驱动”，而是围绕 `ArceOS` 当前的 `VirtIO` 接入层，补齐其在以下方面的工程级能力：

1. 强化 `virtio-net` 的队列记账、buffer 生命周期与异常路径安全性。
2. 强化 `virtio-vsock` 的参数语义、事件路由和连接操作一致性。
3. 规范 `VirtIO` 公共探测、MMIO/PCI glue、HAL 地址转换与平台 IRQ 分层边界。
4. 在不破坏现有架构分层的前提下，让代码更安全、可验证、可维护。

最终合入后的代码，不仅要“能跑”，还要满足 reviewer 对以下方面的要求：

- 内存与 DMA 生命周期不能在错误路径下失控。
- 设备驱动与 OS/platform glue 的边界要清晰。
- 参数语义要与 Linux / VirtIO 既有约定兼容。
- 本地 targeted clippy、单元测试与基础 smoke 路径要能稳定通过。

---

## 3. 主要改动模块

为了和后文结构保持一致，本次最终合并的工作可以归纳为三个核心模块：

### 3.1 `virtio-net`

对应文件：

- `components/axdriver_crates/axdriver_virtio/src/net.rs`
- `os/arceos/modules/axnet-ng/src/device/ethernet.rs`

这一部分主要负责网络收发、队列记账、异常路径回收以及 `TX_DONE` 到上层网络唤醒路径的闭环。

### 3.2 `virtio-vsock`

对应文件：

- `components/axdriver_crates/axdriver_virtio/src/socket.rs`

这一部分主要负责 `vsock` 连接、收发、事件路径以及参数语义与 Linux 行为兼容性。

### 3.3 公共探测、HAL 与 glue

对应文件：

- `components/axdriver_crates/axdriver_virtio/src/lib.rs`
- `components/axdriver_crates/axdriver_virtio/src/input.rs`
- `components/axdriver_crates/axdriver_virtio/src/gpu.rs`
- `os/arceos/modules/axdriver/src/virtio.rs`
- `os/arceos/modules/axdriver/src/bus/mmio.rs`
- `os/arceos/modules/axdriver/src/drivers.rs`
- `os/arceos/modules/axdriver/src/lib.rs`
- `os/arceos/modules/axdriver/src/macros.rs`
- `platform/axplat-dyn/src/drivers/blk/virtio_pci.rs`
- `platform/axplat-dyn/src/drivers/net/virtio_pci.rs`

这一部分主要负责 MMIO probe、PCI IRQ routing、HAL 地址转换，以及 portable driver crate 与 ArceOS / platform 之间的 glue 接线。

---

## 4. `virtio-net`

对应文件：

- `components/axdriver_crates/axdriver_virtio/src/net.rs`

### 4.1 修改原因

原始 `virtio-net` 已经具备基本收发能力，但在工程化层面还存在明显短板：

- RX/TX 队列、free list 和 checked-out buffer 缺少统一记账；
- 多条错误路径会破坏 buffer 生命周期一致性；
- RX 包长校验和 TX completion IRQ 语义不完整。

### 4.2 怎么修改

围绕这些问题，最终代码主要做了三类调整：

- 为 RX/TX 队列建立统一的运行时状态建模与 accounting 校验；
- 在 `transmit_begin()`、`receive_begin()`、`receive_complete()` 等关键路径补齐 rollback、回收和异常包处理；
- 补上严格的 RX 长度校验，以及 `TX_DONE` 到上层 `axnet-ng` 的唤醒闭环。

### 4.3 作用

修改完成后，`virtio-net` 不再只是“能收发”，而是具备了更强的工程稳定性：

- 队列状态可验证，buffer 不容易在错误路径下丢失；
- DMA / buffer 生命周期更安全，不会轻易出现所有权失控；
- 中断驱动发送回收语义完整，上层网络路径更稳定。

---

## 5. `virtio-vsock`

对应文件：

- `components/axdriver_crates/axdriver_virtio/src/socket.rs`

### 5.1 修改原因

原始 `virtio-vsock` 虽然已经具备基本接口，但参数语义和 Linux 兼容性还不够完整，主要体现在：

- `connect/send/recv` 等路径缺少统一抽象；
- 零长度 I/O 语义不对；
- `CID 0` 被错误当成非法 peer，和 `VMADDR_CID_HYPERVISOR` 语义冲突。

### 5.2 怎么修改

最终代码主要做了三件事：

- 用统一的操作建模整理 `connect/send/recv/listen` 等请求路径；
- 收敛参数校验、事件映射和 credit 更新逻辑；
- 修正零长度 I/O 与 `CID 0` 的协议语义，使其更接近 Linux vsock 行为。

### 5.3 作用

修改完成后，`virtio-vsock` 的行为更明确，也更容易被上层系统正确使用：

- 接口语义更稳定，请求路径更统一；
- Linux 常见行为兼容性更好；
- 合法的 hypervisor / host / local CID 不会再被误拒绝。

---

## 6. 公共探测、HAL 与 glue

### 6.1 修改原因

公共探测和 glue 层原本存在两个明显问题：

- MMIO probe helper 过碎，阅读和维护成本较高；
- PCI IRQ routing 放在 portable driver crate 中，分层边界不够清晰。

### 6.2 怎么修改

最终代码围绕这两点做了收敛：

- 把 MMIO probe 的合法性检查和 header / transport 构造收回到 `MmioProbeRegion` 自身；
- 将 `probe_mmio_device()` 简化为更短的主流程；
- 把 PCI IRQ routing 从 `ax-driver-virtio` 移回 ArceOS / platform glue 层，由真正了解平台环境的代码决定中断号。

### 6.3 作用

修改完成后，这部分代码的收益主要体现在：

- 结构更简洁，可读性和可维护性更好；
- reusable driver crate 与 OS glue 的边界更清晰；
- HAL 地址转换和 glue 接线更稳，不容易把平台相关逻辑混进通用驱动层。

---

## 7. `input` / `gpu` 等辅助改动

对应文件：

- `components/axdriver_crates/axdriver_virtio/src/input.rs`
- `components/axdriver_crates/axdriver_virtio/src/gpu.rs`

这部分不是主线改动，但也做了与整体加固方向一致的收尾调整，主要用于避免初始化阶段的脆弱路径，并让 `input`、`gpu` 与新的 probe / glue 结构保持一致。其作用是补齐 `VirtIO` 驱动整体的一致性，避免只有 `net` 和 `vsock` 被加固而其他设备仍停留在旧实现路径上。

---

## 8. 关键验证工作

本次最终合并前，围绕 reviewer 关注点做了多轮本地验证，重点覆盖三类内容：targeted clippy、驱动单元测试，以及基础启动与网络 smoke。

### 8.1 targeted clippy / 单元测试

- `cargo xtask clippy --package ax-driver-virtio`
- `cargo clippy -p ax-driver --no-default-features --features virtio-net -- -D warnings`
- `cargo clippy -p ax-driver --no-default-features --features virtio-socket -- -D warnings`
- `cargo xtask clippy --package axplat-dyn`
- `cargo clippy --manifest-path platform/axplat-dyn/Cargo.toml --features virtio-net-pci -- -D warnings`
- `cargo test -p ax-driver-virtio --features net,socket --lib`

最终阶段，`ax-driver-virtio` 单测达到 `41 passed`。

### 8.2 基本启动与网络 smoke

- `cargo xtask arceos qemu --package ax-helloworld --arch riscv64`
- `cargo xtask arceos test qemu --arch riscv64 --test-group rust --test-case net/udpserver`

关键结果：

- `ax-helloworld` 能正常启动并输出 `Hello, world!`
- `udpserver` 能正常监听并命中成功模式

---

## 9. 成果总结

以最终 merge 进上游的代码为准，本次工作最终形成了以下三方面成果：

1. `virtio-net`：从基础可用提升为队列状态可验证、异常路径安全、生命周期可控的工程化实现。
2. `virtio-vsock`：从接口具备提升为协议语义更明确、Linux 行为更兼容、事件路径更稳定的实现。
3. 公共 `probe / glue / HAL / PCI IRQ`：完成了分层边界整理与结构收敛，降低了 portable driver crate 与平台 glue 的耦合。

因此，这条 PR 的最终价值不只是修复若干具体问题，而是在 ArceOS 现有 VirtIO 驱动体系上，完成了一轮围绕 `virtio-net`、`virtio-vsock` 和公共接入层的系统性加固，使驱动在协议语义、错误路径安全、分层边界和验证闭环上都更加完整、稳定、可维护。
